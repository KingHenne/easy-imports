{
  "name": "easy-imports",
  "displayName": "Easy Imports",
  "description": "Insert relative paths into import statements with fuzzy search autocompletion.",
  "version": "0.0.1",
  "publisher": "Jimdo GmbH",
  "engines": {
    "vscode": "^1.0.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "*"
  ],
  "main": "./out/src/extension",
  "contributes": {},
  "scripts": {
    "vscode:prepublish": "node ./node_modules/vscode/bin/compile",
    "compile": "node ./node_modules/vscode/bin/compile -watch -p ./",
    "postinstall": "node ./node_modules/vscode/bin/install"
  },
  "devDependencies": {
    "typescript": "^1.8.5",
    "vscode": "^0.11.0"
  },
  "dependencies": {
    "fuzzysearch": "^1.0.3"
  }
}
